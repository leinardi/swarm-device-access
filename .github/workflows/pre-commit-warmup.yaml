name: Pre-commit Cache Warm-up

on:
  push:
    branches:
      - main
      - master
    paths:
      - .pre-commit-config.yaml
  workflow_dispatch:

jobs:
  warm-pre-commit-cache:
    uses: leinardi/gh-reusable-workflows/.github/workflows/pre-commit-warmup.yaml@055867b173af35192c4a7d6711b88225b4cf4f48 # v1.2.0
